# オセロゲーム タスク一覧（全フェーズ対応版）

> **開始推奨フェーズ**: まず Phase 1 から始めてください。  
> Phase 1 が完成してから Phase 2 に進むことで、段階的に機能を追加できます。

---

## Phase 1：ローカル2人対戦

> **最小構成**：このフェーズだけでも完全に遊べるオセロゲームが完成します。  
> サーバー不要・ファイル3つだけで動作します。

### [x] 1. index.html の作成
- [x] 1.1 HTMLの基本構造（DOCTYPE, head, body）を作成する
- [x] 1.2 タイトル（h1）を配置する
- [x] 1.3 ターン表示エリア（#turn-display）を配置する
- [x] 1.4 石数表示エリア（#black-count, #white-count）を配置する
- [x] 1.5 盤面コンテナ（#board）を配置する
- [x] 1.6 リセットボタン（#reset-btn）を配置する
- [x] 1.7 結果表示エリア（#result-display）を配置する
- [x] 1.8 style.css と script.js を読み込む

### [x] 2. style.css の作成
- [x] 2.1 body・全体レイアウトのスタイルを定義する
- [x] 2.2 #game-container の中央寄せスタイルを定義する
- [x] 2.3 #board のCSS Gridレイアウト（8×8）を定義する
- [x] 2.4 .cell（各マス）のスタイルを定義する
- [x] 2.5 .cell.valid（置けるマス）のハイライトスタイルを定義する
- [x] 2.6 .stone.black（黒石）のスタイルを定義する
- [x] 2.7 .stone.white（白石）のスタイルを定義する
- [x] 2.8 #reset-btn のスタイルを定義する
- [x] 2.9 #result-display のスタイルを定義する
- [x] 2.10 スマートフォン向けレスポンシブ対応を追加する

### [x] 3. script.js の作成（初期化・盤面描画）
- [x] 3.1 定数（EMPTY, BLACK, WHITE, BOARD_SIZE, DIRECTIONS）を定義する
- [x] 3.2 ゲーム状態変数（board, currentPlayer, gameOver）を定義する
- [x] 3.3 `createBoard()` 関数を実装する（8×8の空配列生成）
- [x] 3.4 `initGame()` 関数を実装する（初期配置・変数リセット）
- [x] 3.5 `renderBoard()` 関数を実装する（HTMLへの盤面反映）
- [x] 3.6 リセットボタンのイベントリスナーを設定する
- [x] 3.7 ページ読み込み時に `initGame()` を呼び出す

### [x] 4. script.js の作成（石を置く・ひっくり返す）
- [x] 4.1 `getFlippedStones(board, row, col, player)` 関数を実装する
  - [x] 4.1.1 8方向ループ処理を実装する
  - [x] 4.1.2 相手の石を挟んでいるか確認する処理を実装する
  - [x] 4.1.3 ひっくり返す石の座標リストを返す処理を実装する
- [x] 4.2 `isValidMove(board, row, col, player)` 関数を実装する
- [x] 4.3 `placeStone(row, col)` 関数を実装する
- [x] 4.4 `flipStones(stones)` 関数を実装する
- [x] 4.5 `handleCellClick(row, col)` 関数を実装する

### [x] 5. script.js の作成（ターン管理・パス・終了判定）
- [x] 5.1 `getValidMoves(player)` 関数を実装する
- [x] 5.2 `countStones()` 関数を実装する
- [x] 5.3 `updateStatus()` 関数を実装する
- [x] 5.4 `switchPlayer()` 関数を実装する（パス処理含む）
- [x] 5.5 `checkGameOver()` 関数を実装する
- [x] 5.6 `showResult()` 関数を実装する

### [x] 6. Phase 1 動作確認
- [x] 6.1 盤面が正しく表示されることを確認する（T1-01）
- [x] 6.2 初期配置が正しいことを確認する（T1-02）
- [x] 6.3 黒から開始することを確認する（T1-03）
- [x] 6.4 置ける場所がハイライトされることを確認する（T1-04）
- [x] 6.5 石を置いたらひっくり返ることを確認する（T1-05）
- [x] 6.6 石数が正しく更新されることを確認する（T1-06）
- [x] 6.7 パス処理が動作することを確認する（T1-07）
- [x] 6.8 ゲーム終了・勝敗表示を確認する（T1-08）
- [x] 6.9 リセットが動作することを確認する（T1-09）
- [x] 6.10 角・辺への着手を確認する（T1-10）
- [x] 6.11 複数方向同時ひっくり返しを確認する（T1-11）

---

## Phase 2：CPU対戦

> **前提**: Phase 1 が完成していること

### [x] 7. 対戦モード選択UIの追加
- [x] 7.1 index.html にモード選択画面（#mode-select）を追加する
- [x] 7.2 「2人対戦」「CPU対戦」ボタンを追加する
- [x] 7.3 CPU対戦選択時に強さ選択UI（初級・中級・上級）を表示する
- [x] 7.4 「ゲーム開始」ボタンを追加する
- [x] 7.5 style.css にモード選択画面のスタイルを追加する

### [x] 8. script.js にモード管理を追加する
- [x] 8.1 `gameMode`・`cpuLevel`・`cpuColor` 変数を追加する
- [x] 8.2 `startGame(mode, level)` 関数を実装する
- [x] 8.3 CPU対戦時に自動でCPUの手を実行する処理を追加する
- [x] 8.4 CPU代理フラグ（`isProxyMode`）を追加する
- [x] 8.5 「CPU代理」ボタンのイベントリスナーを追加する

### [x] 9. cpu.js の作成（AIロジック）
- [x] 9.1 `cpuMove(level)` 関数を実装する（レベルに応じてAIを呼び分ける）
- [x] 9.2 `cpuEasy()` 関数を実装する（ランダム選択）
- [x] 9.3 `cpuMedium()` 関数を実装する（貪欲法：最多ひっくり返し）
- [x] 9.4 `cpuHard()` 関数を実装する（評価関数ベース）
  - [x] 9.4.1 盤面評価関数（角・辺・その他の重み付け）を実装する
  - [ ] 9.4.2 ミニマックス法の再帰処理を実装する ※将来実装予定
  - [ ] 9.4.3 アルファベータ枝刈りを実装する ※将来実装予定
- [x] 9.5 CPUの思考待機時間（0.5秒）を実装する

### [x] 10. Phase 2 動作確認
- [x] 10.1 モード選択画面が表示されることを確認する（T2-01）
- [x] 10.2 初級CPUがランダムに手を選ぶことを確認する（T2-02）
- [x] 10.3 中級CPUが最多ひっくり返しの手を選ぶことを確認する（T2-03）
- [x] 10.4 上級CPUが角を優先することを確認する（T2-04）
- [x] 10.5 CPU代理切り替えが動作することを確認する（T2-05）

> ✅ 確認済み（初級・中級・上級CPU・CPU代理）
> - 2人対戦・CPU対戦（初級・中級・上級）のモード選択が動作する
> - ゲーム開始ボタンでゲーム画面に進める
> - 初級CPUがランダムに手を選ぶ
> - 中級CPUが最多ひっくり返しの手を選ぶ
> - 上級CPUが角・辺を優先し危険マスを避ける
> - CPU思考中は人間がクリックできない
> - CPUターン中は置ける場所のハイライトが表示されない
> - CPUが石を置いた後、人間のターンに戻る
> - CPU代理ボタンで現在の手番を1手だけCPUに任せられる
> - 画面高さに応じて盤面サイズを自動調整済み
> - ゲーム終了時に勝敗表示とボタンが見切れないことを確認済み

---

## Phase 3：戦績管理（ローカル保存）

> **前提**: Phase 2 が完成していること

### [x] 11. stats.js の作成（戦績管理ロジック）
- [x] 11.1 `saveGameResult(result)` 関数を実装する
  - [x] 11.1.1 localStorageから既存データを読み込む
  - [x] 11.1.2 新しい結果を追加する
  - [x] 11.1.3 100件を超えた場合に古いものを削除する
  - [x] 11.1.4 localStorageに保存する
- [x] 11.2 `loadStats()` 関数を実装する
- [x] 11.3 `calcSummary(history, mode)` 関数を実装する（勝率計算）
- [x] 11.4 `resetStats()` 関数を実装する（確認ダイアログ付き）

### [x] 12. 戦績画面UIの追加
- [x] 12.1 index.html に戦績モーダル（#stats-modal）を追加する
- [x] 12.2 戦績サマリー表示エリアを追加する
- [x] 12.3 対戦履歴一覧表示エリアを追加する
- [ ] 12.4 「2人対戦」「CPU対戦」タブ切り替えを追加する
- [x] 12.5 「戦績をリセット」ボタンを追加する
- [x] 12.6 style.css に戦績モーダルのスタイルを追加する

### [x] 13. ゲーム終了時に戦績を自動保存する
- [x] 13.1 `showResult()` 関数内で `saveGameResult()` を呼び出す
- [x] 13.2 保存するデータ（日時・モード・結果・石数）を設定する

### [x] 14. Phase 3 動作確認
- [x] 14.1 対戦結果がlocalStorageに保存されることを確認する（T3-01）
- [x] 14.2 戦績サマリーが正しく計算されることを確認する（T3-02）
- [x] 14.3 対戦履歴が正しく表示されることを確認する（T3-03）
- [x] 14.4 戦績リセットが動作することを確認する（T3-04）
- [x] 14.5 100件超過時に古いものが削除されることを確認する（T3-05）

> ✅ 確認済み（Phase 3 戦績管理）
> - ゲーム終了時に対戦結果が自動保存される
> - 戦績サマリー（対戦数・勝ち・負け・引き分け・勝率）が正しく表示される
> - CPU対戦では「あなたの勝ち/負け」、2人対戦では「黒/白の勝ち」と表示される
> - 上級CPU（hard）の戦績が「CPU（上級）」と正しく表示される
> - 戦績リセットが確認ダイアログ付きで動作する
> - 遊び方モーダルが正しく表示・非表示される

---

## Phase 4：ユーザー登録・ログイン

> **前提**: Phase 3 が完成していること  
> ⚠️ この段階からバックエンドサーバーの構築が必要です

### [ ] 15. バックエンド環境のセットアップ
- [ ] 15.1 Node.js プロジェクトを初期化する（`npm init`）
- [ ] 15.2 必要なパッケージをインストールする（express, bcrypt, jsonwebtoken, pg など）
- [ ] 15.3 PostgreSQL（またはMySQL）のデータベースを作成する
- [ ] 15.4 usersテーブルを作成するSQLを書く
- [ ] 15.5 game_resultsテーブルを作成するSQLを書く
- [ ] 15.6 環境変数ファイル（.env）を作成する（DBの接続情報・JWTシークレットキー）
  - ⚠️ .env ファイルは絶対にGitにコミットしないこと（.gitignoreに追加する）

### [ ] 16. 認証APIの実装（backend/routes/auth.js）
- [ ] 16.1 POST /api/auth/register を実装する
  - [ ] 16.1.1 入力値のバリデーション（ユーザー名・メール・パスワードの形式確認）
  - [ ] 16.1.2 ユーザー名・メールの重複チェック
  - [ ] 16.1.3 パスワードをbcryptでハッシュ化して保存する
- [ ] 16.2 POST /api/auth/login を実装する
  - [ ] 16.2.1 メールアドレスでユーザーを検索する
  - [ ] 16.2.2 bcryptでパスワードを照合する
  - [ ] 16.2.3 JWTトークンを生成して返す
- [ ] 16.3 POST /api/auth/logout を実装する
- [ ] 16.4 GET /api/auth/me を実装する（JWT検証ミドルウェアを使用）

### [ ] 17. フロントエンドに認証UIを追加する
- [ ] 17.1 ログイン画面を作成する
- [ ] 17.2 新規登録画面を作成する
- [ ] 17.3 ログイン状態の管理（JWTトークンをlocalStorageに保存）
- [ ] 17.4 ログアウト機能を実装する
- [ ] 17.5 ログイン済みの場合はゲーム画面を表示する

### [ ] 18. 戦績データをサーバーに保存するよう変更する
- [ ] 18.1 GET /api/stats を実装する（ログインユーザーの戦績を返す）
- [ ] 18.2 POST /api/stats を実装する（対戦結果を保存する）
- [ ] 18.3 フロントエンドの戦績保存処理をAPI呼び出しに変更する

---

## Phase 5：カジュアルなネット対戦

> **前提**: Phase 4 が完成していること  
> ⚠️ WebSocketサーバーの構築が必要です

### [ ] 19. WebSocketサーバーのセットアップ
- [ ] 19.1 Socket.io をインストールする
- [ ] 19.2 server.js に Socket.io を組み込む
- [ ] 19.3 ルーム管理のデータ構造を設計・実装する

### [ ] 20. ルーム作成・参加機能の実装
- [ ] 20.1 `create_room` イベントを実装する（サーバー側）
- [ ] 20.2 `join_room` イベントを実装する（サーバー側）
- [ ] 20.3 `random_match` イベントを実装する（サーバー側）
- [ ] 20.4 フロントエンドにルーム作成・参加UIを追加する

### [ ] 21. リアルタイム対戦の実装
- [ ] 21.1 `place_stone` イベントを実装する（サーバー側でゲームロジックを検証）
- [ ] 21.2 `board_updated` イベントをクライアントに送信する
- [ ] 21.3 `game_over` イベントを実装する
- [ ] 21.4 切断時の処理（`opponent_disconnected` イベント）を実装する
- [ ] 21.5 フロントエンドの盤面更新処理をWebSocket受信に対応させる

### [ ] 22. カジュアル対戦の戦績保存
- [ ] 22.1 ゲーム終了時にカジュアル対戦の結果をDBに保存する
- [ ] 22.2 戦績画面にカジュアル対戦タブを追加する

---

## Phase 6：ランク対戦・レート・マッチング

> **前提**: Phase 5 が完成していること

### [ ] 23. レーティングシステムの実装
- [ ] 23.1 ratingsテーブルを作成するSQLを書く
- [ ] 23.2 ranked_game_resultsテーブルを作成するSQLを書く
- [ ] 23.3 Eloレーティング計算関数を実装する
- [ ] 23.4 ゲーム終了時にレートを更新する処理を実装する
- [ ] 23.5 レートに応じてランクを付与する処理を実装する

### [ ] 24. ランクマッチングの実装
- [ ] 24.1 マッチングキューの管理ロジックを実装する
- [ ] 24.2 レート差による段階的な許容範囲拡大を実装する
- [ ] 24.3 マッチング成立時にゲームを開始する処理を実装する

### [ ] 25. ランク対戦UIの追加
- [ ] 25.1 「ランク対戦」ボタンをゲームメニューに追加する
- [ ] 25.2 マッチング待機画面を作成する（待機時間の表示）
- [ ] 25.3 プロフィール画面にランク・レートを表示する
- [ ] 25.4 ランキング表示画面を作成する（上位100名）

### [ ] 26. ランク戦績の管理
- [ ] 26.1 GET /api/ranked-stats を実装する
- [ ] 26.2 戦績画面にランク対戦タブを追加する
- [ ] 26.3 レート変動履歴を表示する機能を追加する
